# Evita que avisos do ESLint falhem o build no deploy (Coolify, Docker, etc.)
CI=false
GENERATE_SOURCEMAP=false
# Desabilita ESLint durante o build para não gerar "Compiled with warnings" (evita falha em pipelines)
DISABLE_ESLINT_PLUGIN=true
